[flake8]
max-line-length = 88

# Only check tests, but everywhere.
# This still may skip files used in the tests depending on the glob function.
filename =
    */testsuite/*.py,

exclude =
    .git,
    __pycache__,
    .env,
    .venv,
    env,
    venv,
    ENV,
    env.bak,
    venv.bak,
    bin.*,
    dist.*,
